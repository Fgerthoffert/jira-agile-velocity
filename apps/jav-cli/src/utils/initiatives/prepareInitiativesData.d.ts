import { IConfig } from '../../global';
declare const prepareInitiativesData: (issuesTree: any, node: any, level: number, closedIssues: any[], emptyCalendar: any, userConfig: IConfig) => never[];
export default prepareInitiativesData;
