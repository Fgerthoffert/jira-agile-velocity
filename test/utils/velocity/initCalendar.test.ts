// tslint:disable-next-line: file-name-casing
import * as assert from "assert";

import initCalendar from "../../../src/utils/velocity/initCalendar";

describe("Array", function() {
  describe("initCalendar()", function() {
    it("Dummy test, to be replaced", function() {
      //console.log(new Date("2019-07-10"));
      //      console.log(initCalendar(new Date("2019-07-10"), new Date("2019-07-15")));
      assert.equal([1, 2, 3].indexOf(4), -1);
    });
  });
});
