// tslint:disable-next-line: file-name-casing
import React from 'react';
import { Redirect } from 'react-router-dom';

const RedirectVelocity: React.FC = () => {
  return <Redirect to='/velocity' />;
};

export default RedirectVelocity;
